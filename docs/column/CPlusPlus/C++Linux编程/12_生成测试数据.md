# 生成测试数据

## 测试数据的业务需求

程序员必须熟悉业务

- 银行系统:存款和取款的流程，利息的计算方法;
- 医院系统:挂号、门诊、住院、药房、收费等流程和数据的含义。
- 游戏开发:先会玩游戏。



在中国，程序员的工作以应用系统的开发为主;

- 在中国，程序员的工作以应用系统的开发为主;
- 所以，几乎没有只会写程序，不熟悉业务的程序员;
- 找工作面试的时候，除了聊技术，也可能聊业务，业务是工作经验的一部分。
- 数据开放平台的业务相关性不强，数据很多，但不必深入了解;
- 两种数据用例:全国气象站点参数、全国气象站点观测数据。
- 课程的最后一章会介绍更多的数据以及获取数据的方法，应对面试。

## 测试数据用例

- 全国气象站点参数（已准备好，存于文件中)。

```
vim /project/idc/ini/stcode.ini
```



![image-20240331101618440](https://blogwnx-bucket.oss-cn-beijing.aliyuncs.com/img/image-20240331101618440-17118513799421.png)

全国气象站点观测数据（需要写程序生成测试数据文件，生成后的数据存于文件中，包括csv、xml、json三种格式)。



测试数据生成如何实现？这里以超级女生信息为例子。假设我们需要生成这样的列表数据。

![image-20240331101803613](https://blogwnx-bucket.oss-cn-beijing.aliyuncs.com/img/image-20240331101803613-17118514846192.png)



> 数据分析：数据每一行可以用结构体变量表示，每一个字段可以作为结构体的成员.



针对字段说明: no(编号)、name(姓名)、height(身高)、sc(身材)、yz（颜值)。



**CSV**

```shell
no,name,height,sc,yz
0001,西施,165,火辣,沉鱼
0002,貂蝉,168,匀称,落雁
0003,冰冰,167,丰满,漂亮
```



:::tip CVS文件内容特点：

- 第一行表示字段名的内容可有可无;

- 每一行代表一条记录，字段之间用逗号分隔;

- 字段的取值中不能包含逗号，否则有歧义。

:::



 **xml**

```xml
<girls>
    <no>0001</no>
    <name>西施</name>
    <height>165</height>
    <sc>火辣</sc>
    <yz>沉鱼</yz>
    <end/>
    <no>0002</no>
    <name>貂蝉</name>
    <height>168</height>
    <sc>匀称</sc>
    <yz>落雁<lyz>
    <end/>
    <no>0003</no>
    <name>冰冰</name>
    <height>167</height>
    <sc>丰满</sc>
    <yz>漂亮</yz>
    <end/>
</girls>
```

- 数据集的开始和结束的标签: `<grils> </girls>`
- 每行数据结束的标签: ` <endl/>`
- 字段标签: no(编号)、name (姓名)、height(身高)、sc (身材)、yz（颜值)



::: tip xml文件内容特点：

- 可扩展标记语言，标准通用标记语言的子集，简称XML。
- 自我描述，简单易懂，容错性和可扩展性非常好。
- 是数据处理、数据通讯和数据交换等应用场景的常用数据格式。

:::



**json**

```json
{
"girls":
[ 
  {"no":"0001" ,"name":"西施" , "height":"165" ,"sc":"火辣" ,"yz":"沉鱼"},

 {"no":"0002","name":"貂蝉" ,"height":"168" ,"sc":"匀称","yz":"落雁"},
    
 {"no":"0003" ," name"∵"冰冰" , " height":"167" ,"sc":"丰 满","yz":"漂亮"}
]
}
```

girls对象包含了三个超女记录的数组。



::: tip json文件内容特点：

- JavaScript Object Notation，JavaScript对象表示法。
- 是一种轻量级的文本数据交换格式。
- 使用Javascript语法来描述数据对象，自我描述，容易理解。

:::



xml和json有点复杂，大部分的场景采用最简单的格式即可。

xml和json是一种思想，可随意发挥，实用就好，不必拘泥于形式。

不一定要采用标准的xml或json格式。



::: warning 业务需求：**气象站点观测数据**

- 气象站一分钟进行一次观测，每观测一次产生一条数据。
- 一共有839个站点，每分钟将生成839条数据。
- 这里产生的数据，分别用csv、 xml、 json 格式化之后，保存到磁盘文件中。

:::

## 搭建程序的框架

学习方法：

- 先把一节视频看完，不能完全理解为什么这么做也没关系。
- 把代码默写出来，编译、调试、实现功能。(初学阶段不要有自己的想法)
- 一步一个脚印，搞定了一节课再继续下一节课。(环环相扣)
- 目录结构、文件名与我保持相同，避免没必要的麻烦。

```c
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;


int main(int argc,char *argv[])
{
    // 站点参数文件  生成的测试数据存放的目录 本程序运行的日志
    if (argc!=4)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";

        return -1;  
    }
}
```

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# ./crtsurfdata 
Using:./crtsurfdata inifile outpath logfile
Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log

inifile  气象站点参数文件名。
outpath  气象站点数据文件存放的目录。
logfile  本程序运行的日志文件名。
[root@localhost cpp]# 
```



### 整合日志记录

:::details 落地实现

将日志对象`clogfile` 定义为全局变量，并使用`write()` 方法进行日志记录。

```c{27,31}
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
clogfile logfile;          // 本程序运行的日志。
int main(int argc,char *argv[])
{
    // 站点参数文件  生成的测试数据存放的目录 本程序运行的日志
    if (argc!=4)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        return -1;  
    }
    if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  return -1;
    }

    logfile.write("crtsurfdata 开始运行。\n");
    
     // 在这里编写处理业务的代码。

    logfile.write("crtsurfdata 运行结束。\n");

    return 0;

}
```

编译源码，并运行`crtsurfdata` 程序。由于当前用户没有对`/project/idc/bin/crtsurfdata `文件有执行权限，我们可以通过`chomd +x` 给此文件赋予执行权限。运行完毕之后，通过`more` 指令查看日志文件内容。

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
-bash: /project/idc/bin/crtsurfdata: 权限不够
[root@localhost cpp]# chmod +x /project/idc/bin/crtsurfdata 
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
[root@localhost cpp]# more /log/idc/crtsurfdata.log 
2024-03-30 19:42:49 crtsurfdata 开始运行。
2024-03-30 19:42:49 crtsurfdata 运行结束。
[root@localhost cpp]# 
```

可以看到`write()` 方法注入的文件内容被成功写入了。

::: 



证实我们成功整合了日志记录。

### 整合信号处理

:::details 落地实现

```c{8,25-27,37,45-50}
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
clogfile logfile;         
void EXIT(int sig);      // 程序退出和信号2、15的处理函数。
int main(int argc,char *argv[])
{
    // 站点参数文件  生成的测试数据存放的目录 本程序运行的日志
    if (argc!=4)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        return -1;  
    }
    
	// 关闭0、1、2和忽略全部的信号。
    closeioandsignal(true);   
    //处理2、15的信号
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);

     if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  return -1;
    }

    logfile.write("crtsurfdata 开始运行。\n");
    
    // 在这里编写处理业务的代码。
    sleep(100);

    logfile.write("crtsurfdata 运行结束。\n");

    return 0;

}
// 程序退出和信号2、15的处理函数。
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);

    exit(0);
}
```

编译源码，并`crtsurfdata`程序，程序因`sleep(100)`的缘故被线程阻塞，进程将在后台不断运行，此时按`ctrl+c` 并查看日志，可以看到进程收到了2的信号后，终止了进程。

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
^C[root@localhost cpp]# more /log/idc/crtsurfdata.log 
2024-03-30 19:40:00 crtsurfdata 开始运行。
2024-03-30 19:40:23 crtsurfdata 开始运行。
2024-03-30 19:40:23 crtsurfdata 运行结束。
2024-03-30 19:42:49 crtsurfdata 开始运行。
2024-03-30 19:42:49 crtsurfdata 运行结束。
2024-03-30 19:48:31 crtsurfdata 开始运行。
2024-03-30 19:49:15 程序退出，sig=2
```

再一次运行`crtsurfdata`程序。

```shell
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
```

并在另一个窗口，通过 `killall -15` 的指令 向进程发送15的信号。进程收到了15的信号之后，同样进程终止了。

```shell
连接主机...
连接主机成功
Last login: Sat Mar 30 19:11:54 2024 from 192.168.28.1
[root@localhost ~]# killall -15 crtsurfdata
```

通过`more` 指令，查看该`crtsurfdata`程序运行的日志文件，可以看到。进程确实是收到了2、15的信号后，执行了`EXIT()` 函数的逻辑后，进程终止了。

```c
[root@localhost cpp]# more /log/idc/crtsurfdata.log 
2024-03-30 19:40:00 crtsurfdata 开始运行。
2024-03-30 19:40:23 crtsurfdata 开始运行。
2024-03-30 19:40:23 crtsurfdata 运行结束。
2024-03-30 19:42:49 crtsurfdata 开始运行。
2024-03-30 19:42:49 crtsurfdata 运行结束。
2024-03-30 19:48:31 crtsurfdata 开始运行。
2024-03-30 19:49:15 程序退出，sig=2

2024-03-30 19:50:13 crtsurfdata 开始运行。
2024-03-30 19:50:48 程序退出，sig=15
```

:::

我们成功的整合了信号处理。

## 加载站点数据程序

为了完成我们的测试数据的业务需求，笔者将实现步骤分为如下三步走，并依次按照步骤编码，进行落地编码。



:::tip 需求实现步骤分析

- 从站点参数文件中加载站点参数，存放于stlist容器中；
- 根据站点参数，生成站点观测数据（随机数）
- 把站点观测数据存保到文件中

:::



### 使用cifile类读取站点数据

:::details 落地实现

```c{53}
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
struct st_stcode; // 站点参数的结构体

// 直接把站点数据抄写表头和一行记录过来，便于结构体抽象
// 省   站号  站名 纬度   经度  海拔高度
// 安徽,58015,砀山,34.27,116.2,44.2
struct st_stcode    
{
    //string操作方便、自动扩容
    //但是操作数据库时，string没有优势，所以这里采用char []
    char provname[31]; // 省
    char obtid[11]; // 站号
    char obtname[31]; // 站名，同城市名
    double lat; // 纬度：度
    double lon; // 经度：度
    double height;// 海拔高度：米
};

clogfile logfile;
list<struct st_stcode> stlist;  // 存全部的站点参数
// 把站点参数文件加载到stlist容器中
bool loadstcode(const string &inifile);  
// 程序退出和信号2、15的处理函数。       
void EXIT(int sig);     
int main(int argc,char *argv[])
{
    if (argc!=4)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        return -1;  
    }
    closeioandsignal(true);     
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);
    if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  			return -1;
    }
    logfile.write("crtsurfdata 开始运行。\n");
    

    // 1）从站点参数文件中加载站点参数，存放于stlist容器中
    if (loadstcode(argv[1]) ==  false) EXIT(-1);   
    
    // 2）根据站点参数，生成站点观测数据（随机数）

    // 3）把站点观测数据存保到文件中

    logfile.write("crtsurfdata 运行结束。\n");

    return 0;

}
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);

    exit(0);
}
bool loadstcode(const string &inifile)   
{
   cifile ifile;       // 读取文件的对象
   string strbuffer;  // 存文件中读取的每一行   
   if (ifile.open(inifile)==false) 
   {
       logfile.write("ifile.open(%s) failed.\n",inifile.c_str()); 
   	   return false;
   }
   // 读取站点参数文件的第一行，它是标题，扔掉。
   ifile.readline(strbuffer);        

   while(ifile.readline(strbuffer))
   {
        logfile.write("strbuffer=%s\n",strbuffer.c_str());
   }
}
```

使用`make` 指令便于源码，并运行`crtsurfdata`程序。程序运行结束之后，通过`more`指令 查看程序日志，逐一检查输入写入行数和文件本身行数是否一致，如无差错则可以知道，本次文件内容读取到内存没有问题。

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# rm -rf /log/idc/crtsurfdata.log 
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
[root@localhost cpp]# more /log/idc/crtsurfdata.log
2024-03-30 20:00:59 crtsurfdata 开始运行。
2024-03-30 20:00:59 strbuffer=安徽,58015,砀山,34.27,116.2,44.2
2024-03-30 20:00:59 strbuffer=安徽,58102,亳州,33.47,115.44,39.1
2024-03-30 20:00:59 strbuffer=安徽,58118,蒙城,33.16,116.31,10.3
```

:::

### 使用ccmdstr拆分文件行

:::details `ccmdstr` 将所有行数据 载入`stlist`容器

```c{89-101}
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
struct st_stcode;
struct st_stcode    
{
    char provname[31];     
    char obtid[11];            
    char obtname[31];     
    double lat;                 
    double lon;                
    double height;           
};
list<struct st_stcode> stlist;  // 存全部的站点参数
clogfile logfile;          // 本程序运行的日志

// 把站点参数文件加载到stlist容器中
bool loadstcode(const string &inifile);   
// 程序退出和信号2、15的处理函数
void EXIT(int sig);      
int main(int argc,char *argv[])
{
    // 站点参数文件  生成的测试数据存放的目录 本程序运行的日志
    if (argc!=4)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        return -1;  
    }


    closeioandsignal(true);    
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);

     if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n"; 
        return -1;
    }

    logfile.write("crtsurfdata 开始运行。\n");
    
    // 在这里编写处理业务的代码。
    // 1）从站点参数文件中加载站点参数，存放于stlist容器中；
    if (loadstcode(argv[1]) ==  false) EXIT(-1);   

    // 2）根据站点参数，生成站点观测数据（随机数）；

    // 3）把站点观测数据存保到文件中。

    logfile.write("crtsurfdata 运行结束。\n");

    return 0;

}
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);

    exit(0);
}
bool loadstcode(const string &inifile)   
{
   cifile ifile;  
   string strbuffer;   
   ccmdstr cmdstr;  // 用于拆分从文件中读取的行的对象 
   st_stcode stcode; // 站点参数的结构体
   if (ifile.open(inifile)==false) 
    {
        logfile.write("ifile.open(%s) failed.\n",inifile.c_str());
        return false;
    }

        
    ifile.readline(strbuffer);     
    while(ifile.readline(strbuffer))
    {
        
        // 拆分从文件中读取的行
        //例如：安徽,58015,砀山,34.27,116.2,44.2=> push_back vector
        cmdstr.splittocmd(strbuffer,",");  
        
		//把拆分项逐一填充到站点参数结构体中
        memset(&stcode,0,sizeof(st_stcode));
        cmdstr.getvalue(0,stcode.provname,30); // 省
        cmdstr.getvalue(1,stcode.obtid,10);// 站点代码
        cmdstr.getvalue(2,stcode.obtname,30);// 站名
        cmdstr.getvalue(3,stcode.lat); // 纬度
        cmdstr.getvalue(4,stcode.lon); // 经度
        cmdstr.getvalue(5,stcode.height);// 海拔高度
     
        // 把站点参数存入stlist容器中
        stlist.push_back(stcode); 
    }
    //tip: 这里不需要手工关闭文件，cifile类的析构函数会关闭文件
    
    //将结果写入到日志文件中,以验证加载站点参数到slist容器中成功
    for (auto &aa:stlist)
    {       logfile.write("provname=%s,obtid=%s,obtname=%s,lat=%.2f,lon=%.2f,height=%.2f\n",\                             aa.provname,aa.obtid,aa.obtname,aa.lat,aa.lon,aa.height);
    }
    return 0;
}
```

通过`make` 指令编译源码文件，并运行`crtsurfdata`程序后,程序运行结束，通过`more`指令查看日志文件。

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# rm -rf /log/idc/crtsurfdata.log 
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
[root@localhost cpp]# more /log/idc/crtsurfdata.log
2024-03-30 20:11:33 provname=河北,obtid=54705,obtname=南宫,lat=37.22,lon=115.23,height=27.40
....
```

:::

可以看到站点文件数据，成功加载进来内存。有序存放在我们定义的结构体中。到这里，我们第一步完成。继续进行第二步的编码工作，即根据站点参数，生成站点观测数据（随机数）。

### 测试模拟的站点生成数据

:::details `crtsurfdata()` 落地实现

```c{72-81,126-147}
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
struct st_stcode;  // 站点参数的结构体
struct st_surfdata;// 气象站观测数据的结构体
struct st_stcode  
{
    char provname[31]; 
    char obtid[11];           
    char obtname[31];      
    double lat;                 
    double lon;               
    double height;           
};
struct st_surfdata
{
    char obtid[11]; // 站点代码。
    char ddatetime[15];  // 数据时间：格式yyyymmddhh24miss，精确到分钟，秒固定填00。
    int  t; // 气温：单位，0.1摄氏度。
    int  p; // 气压：0.1百帕。
    int  u; // 相对湿度，0-100之间的值。
    int  wd;// 风向，0-360之间的值。
    int  wf; // 风速：单位0.1m/s
    int  r; // 降雨量：0.1mm。
    int  vis; // 能见度：0.1米。
};

list<struct st_surfdata>  datalist; // 存放观测数据的容器
list<struct st_stcode> stlist;// 存全部的站点参数
char strddatetime[15];//当前操作系统时间格式字符串
clogfile logfile;         

// 把站点参数文件加载到stlist容器中
bool loadstcode(const string &inifile);   
// 根据stlist容器中的站点参数，生成站点观测数据，存放在datalist中
void crtsurfdata();  
// 程序退出和信号2、15的处理函数
void EXIT(int sig);   
int main(int argc,char *argv[])
{
    // 站点参数文件  生成的测试数据存放的目录 本程序运行的日志
    if (argc!=4)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        return -1;  
    }

    closeioandsignal(true);      
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);
    if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  			return -1;
    }

    logfile.write("crtsurfdata 开始运行。\n");
    
    // 在这里编写处理业务的代码
    // 1）从站点参数文件中加载站点参数，存放于stlist容器中；
    if (loadstcode(argv[1]) ==  false) EXIT(-1);   


    // 2）根据站点参数，生成站点观测数据（随机数）
    memset(strddatetime,0,sizeof(strddatetime));
    
    // 获取系统当前时间
    ltime(strddatetime,"yyyymmddhh24miss");   
    
    // 把数据时间中的秒固定填00
    strncpy(strddatetime+12,"00",2);                  
    
    //根据stlist容器中的站点参数，生成站点观测数据，存放在datalist中
    crtsurfdata();

    // 3）把站点观测数据存保到文件中。
    
    logfile.write("crtsurfdata 运行结束。\n");

    return 0;

}
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);
    exit(0);
}
bool loadstcode(const string &inifile)   
{
    cifile ifile;  
    string strbuffer;       
	ccmdstr cmdstr;                  
    st_stcode stcode;    
    if (ifile.open(inifile)==false) 
    {
        logfile.write("ifile.open(%s) failed.\n",inifile.c_str()); 
        return false;
    }
	//加载站点数据文件到stlist中
    ifile.readline(strbuffer);         
    while(ifile.readline(strbuffer))
    {
        cmdstr.splittocmd(strbuffer,",");         
        memset(&stcode,0,sizeof(st_stcode));
        cmdstr.getvalue(0,stcode.provname,30);  
        cmdstr.getvalue(1,stcode.obtid,10);          
        cmdstr.getvalue(2,stcode.obtname,30);  
        cmdstr.getvalue(3,stcode.lat);                 
        cmdstr.getvalue(4,stcode.lon);                  
        cmdstr.getvalue(5,stcode.height);            
        stlist.push_back(stcode);                           
    }
    return true;
}

// 模拟生成气象分钟观测数据，存放在datalist容器中
void crtsurfdata()
{
    srand(time(0));// 播随机数种子
    st_surfdata stsurfdata;   // 观测数据的结构体
    for (auto &aa : stlist)
    {
        memset(&stsurfdata,0,sizeof(st_surfdata));
        // 填充观测数据的结构体的成员。
        strcpy(stsurfdata.obtid,aa.obtid); // 站点代码
        strcpy(stsurfdata.ddatetime,strddatetime);// 数据时间
   		//气温：单位，0.1摄氏度。   [0-350)之间
        //可犯可不犯的错误不要犯。
        //就算是假数据也要符合实际来。
        //不要让别人感觉到你这个人很不专业，惹得闲话。
        stsurfdata.t=rand()%350;                                 
        stsurfdata.p=rand()%265+10000;// 气压：0.1百帕
        stsurfdata.u=rand()%101;  // 相对湿度， [0-100]之间的值。
        stsurfdata.wd=rand()%360; // 风向，[0-360)之间的值。
        stsurfdata.wf=rand()%150; // 风速：单位0.1m/s。
        stsurfdata.r=rand()%16;  // 降雨>量：0.1mm。
        stsurfdata.vis=rand()%5001+100000; // 能见度：0.1米。
        // 把观测数据的结构体放入datalist容器                        
        datalist.push_back(stsurfdata);     
    }
	//验证我们datalist是否达到了业务需要
    for (auto &aa:datalist)
    {
        logfile.write("%s,%s,%.1f,%.1f,%d,%d,%.1f,%.1f,%.1f\n", \                              aa.obtid,aa.ddatetime,aa.t/10.0,aa.p/10.0,aa.u,aa.wd,aa.wf/10.0,aa.r/10.0,aa.vis/10.0);
    }
}
```

重复编译工作，可以看到`datalist`数据已经符合我们预期了。

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# rm -rf /log/idc/crtsurfdata.log 
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log
2024-03-30 20:30:28 crtsurfdata 开始运行。
2024-03-30 20:30:28 58015,20240330203000,33.7,1013.1,88,22,14.9,0.3,10189.0
2024-03-30 20:30:28 58102,20240330203000,14.5,1015.2,60,22,14.9,1.3,10030.1
```

:::

到这里，我们第二步完成了，即根据站点参数，生成站点观测数据（随机数）。下面进行第三步的代码编写工作，即把数据写入文件。

### 把数据写入文件

#### 写成csv格式

:::details 落地实现

```c
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
struct st_stcode;// 站点参数的结构体
struct st_surfdata;// 气象站观测数据的结构体
struct st_stcode    
{
    char provname[31];// 省
    char obtid[11]; // 站号
    char obtname[31];// 站名，同城市名
    double lat; // 纬度：度
    double lon;// 经度：度
    double height;// 海拔高度：米
};
struct st_surfdata
{
    char obtid[11];// 站点代码。
    char ddatetime[15];  // 数据时间：格式yyyymmddhh24miss，精确到分钟，秒固定填00。
    int  t; // 气温：单位，0.1摄氏度。
    int  p; // 气压：0.1百帕。
    int  u; // 相对湿度，0-100之间的值。
    int  wd; // 风向，0-360之间的值。
    int  wf; // 风速：单位0.1m/s
    int  r; // 降雨量：0.1mm。
    int  vis;// 能见度：0.1米。
};

clogfile logfile; 
list<struct st_surfdata>  datalist;//存观测数据的容器
list<struct st_stcode> stlist;//存放全部的站点参数
char strddatetime[15]; // 数据时间：格式yyyymmddhh24miss，精确到分钟，秒固定填00。
         
//把站点参数文件加载到stlist容器中
bool loadstcode(const string &inifile);
//根据stlist容器中的站点参数，生成站点观测数据，存放在datalist中
void crtsurfdata(); 
// 程序退出和信号2、15的处理函数
void EXIT(int sig);   
/*===========把容器datalist中的气象观测数据写入文件==========
 	const string& outpath	//<I> 数据文件存放的目录
 	const string& datafmt	//<I> 数据文件的格式 取值：csv、xml和json
*/
bool crtsurffile(const string& outpath,const string& datafmt);
int main(int argc,char *argv[])
{
    if (argc!=5)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile datafmt\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log csv,xml,json\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        cout << "datafmt  输出数据文件的格式，支持csv、xml和json，中间用逗号分隔。\n\n";
        return -1;  
    }
    closeioandsignal(true);       
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);
    if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  return -1;
    }
    logfile.write("crtsurfdata 开始运行。\n");
    
    // 在这里编写处理业务的代码。
    // 1）从站点参数文件中加载站点参数，存放于stlist容器中；
    if (loadstcode(argv[1]) ==  false) EXIT(-1);   
    // 2）根据站点参数，生成站点观测数据（随机数）；
    memset(strddatetime,0,sizeof(strddatetime));
    ltime(strddatetime,"yyyymmddhh24miss");   
    strncpy(strddatetime+12,"00",2); // 把数据时间中的秒固定填00。
    crtsurfdata();
    // 3）把容器datalist中的气象观测数据写入文件
    if (strstr(argv[4],"csv")!=0)    crtsurffile(argv[2],"csv");
    logfile.write("crtsurfdata 运行结束。\n");
    return 0;
}

// 程序退出和信号2、15的处理函数。
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);

    exit(0);
}
bool loadstcode(const string &inifile)   
{
    cifile ifile; 
    string strbuffer;      
    ccmdstr cmdstr;                    
    st_stcode stcode;   
    if (ifile.open(inifile)==false) 
    {
        logfile.write("ifile.open(%s) failed.\n",inifile.c_str()); return false;
    }
    ifile.readline(strbuffer);         
    while(ifile.readline(strbuffer))
    {      
        cmdstr.splittocmd(strbuffer,",");        
        memset(&stcode,0,sizeof(st_stcode));
        cmdstr.getvalue(0,stcode.provname,30);   
        cmdstr.getvalue(1,stcode.obtid,10);           
        cmdstr.getvalue(2,stcode.obtname,30);     
        cmdstr.getvalue(3,stcode.lat);                    
        cmdstr.getvalue(4,stcode.lon);                  
        cmdstr.getvalue(5,stcode.height);             
        stlist.push_back(stcode);                            
    }
    return true;
}
void crtsurfdata()
{
    srand(time(0));              
    st_surfdata stsurfdata;  
    for (auto &aa : stlist)
    {	//基于随机数 做假数据
        memset(&stsurfdata,0,sizeof(st_surfdata));。
        strcpy(stsurfdata.obtid,aa.obtid);                      
        strcpy(stsurfdata.ddatetime,strddatetime);      
        stsurfdata.t=rand()%350;                                 
        stsurfdata.p=rand()%265+10000;                      
        stsurfdata.u=rand()%101;                                
        stsurfdata.wd=rand()%360;                               
        stsurfdata.wf=rand()%150;                               
        stsurfdata.r=rand()%16;                                
        stsurfdata.vis=rand()%5001+100000;             
        datalist.push_back(stsurfdata);
    }
}
bool crtsurffile(const string& outpath,const string& datafmt)
{
      string strfilename;//数据文件名，绝对路径
      cofile ofile;         // 写出数据文件的对象
    
    // 拼接生成数据的文件名
    //例如：/tmp/idc/surfdata/SURF_ZH_20210629092200_2254.csv
    strfilename=outpath+"/"+"SURF_ZH_"+strddatetime+"_"+
    to_string(getpid())+"."+datafmt;
    if (ofile.open(strfilename)==false)
    {
        logfile.write("ofile.open(%s) failed.\n",strfilename.c_str()); return false;
    }
    
    // 把datalist容器中的观测数据写入文件，支持csv、xml和json三种格式。
    if (datafmt=="csv")    
        ofile.writeline("站点代码,数据时间,气温,气压,相对湿度,风向,风速,降雨量,能见度\n");
    
    // 遍历存放观测数据的datalist容器
    for (auto &aa : datalist)
    {
        // 把每行数据写入文件。
         if (datafmt=="csv") 
         {
              ofile.writeline("%s,%s,%.1f,%.1f,%d,%d,%.1f,%.1f,%.1f\n",                                		aa.obtid,
                              aa.ddatetime,
                              aa.t/10.0,
                              aa.p/10.0,
                              aa.u,
                              aa.wd,
                              aa.wf/10.0,
                              aa.r/10.0,
                              aa.vis/10.0);
         }
    }
     // 关闭临时文件，并改名为正式的文件
    ofile.closeandrename();   
	
    //将生成的csv文件路径、什么时候生成的、并且有多少条写入日志，编译开发分析。上线后这一行不要记录。
    logfile.write("生成数据文件%s成功，数据时间%s，记录数%d。\n",strfilename.c_str(),strddatetime,datalist.size());
    return true;
}
```

通过`make` 指令编译源码，并运行`crtsurfdata`程序，并查看日志文件，以及生成的csv文件，比对日志中的数据行数和csv文件内容的数据行数是否一致，数据内容是否发生了错乱，并且csv文件名是否有效符合拼接预期。

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# rm -rf /log/idc/crtsurfdata.log 
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log csv,xml,json
[root@localhost cpp]# cd /tmp/idc/surfdata/
[root@localhost surfdata]# ls
SURF_ZH_20240330204800_89591.csv
[root@localhost surfdata]# more /log/idc/crtsurfdata.log 
2024-03-30 20:48:28 crtsurfdata 开始运行。
2024-03-30 20:48:28 生成数据文件/tmp/idc/surfdata/SURF_ZH_20240330204800_89591.csv成功，数据时间20240330204800，记录数839。
2024-03-30 20:48:28 crtsurfdata 运行结束。
[root@localhost surfdata]# more SURF_ZH_20240330204800_89591.csv
站点代码,数据时间,气温,气压,相对湿度,风向,风速,降雨量,能见度
58015,20240330204800,2.4,1014.3,53,318,12.7,1.1,10223.6
58102,20240330204800,3.4,1017.9,16,192,8.7,1.0,10414.6
58118,20240330204800,6.2,1008.2,16,187,12.3,1.3,10145.5
....
```

均无误，证明我们写成将`datalist` 写成csv文件的逻辑没有问题。

:::

#### 写成xml格式

:::details 落地实现

```c
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
struct st_stcode;// 站点参数的结构体
struct st_surfdata;// 气象站观测数据的结构体
struct st_stcode    
{
    char provname[31]; // 省
    char obtid[11]; // 站号
    char obtname[31]; // 站名，同城市名。
    double lat;// 纬度：度。
    double lon;// 经度：度。
    double height; // 海拔高度：米。
};

struct st_surfdata
{
    char obtid[11]; // 站点代码。
    char ddatetime[15];  // 数据时间：格式yyyymmddhh24miss，精确到分钟，秒固定填00。
    int  t;// 气温：单位，0.1摄氏度。
    int  p;// 气压：0.1百帕。
    int  u;// 相对湿度，0-100之间的值。
    int  wd;// 风向，0-360之间的值。
    int  wf;// 风速：单位0.1m/s
    int  r; // 降雨量：0.1mm。
    int  vis;// 能见度：0.1米。
};
// 存观测数据的容器
list<struct st_surfdata>  datalist;         
// 存放全部的站点参数
list<struct st_stcode> stlist;
char strddatetime[15];   
clogfile logfile;      

/*===========把容器datalist中的气象观测数据写入文件==========
 	const string& outpath	//<I> 数据文件存放的目录
 	const string& datafmt	//<I> 数据文件的格式 取值：csv、xml和json
*/
bool crtsurffile(const string& outpath,const string& datafmt);
bool loadstcode(const string &inifile);  
void crtsurfdata(); 
void EXIT(int sig);      
int main(int argc,char *argv[])
{
    if (argc!=5)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile datafmt\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log csv,xml,json\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        cout << "datafmt  输出数据文件的格式，支持csv、xml和json，中间用逗号分隔。\n\n";

        return -1;  
    }


    // 设置信号,在shell状态下可用 "kill + 进程号" 正常终止些进程。
    // 但请不要用 "kill -9 +进程号" 强行终止。
    closeioandsignal(true);       // 关闭0、1、2和忽略全部的信号。
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);
    if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  			return -1;
    }
    logfile.write("crtsurfdata 开始运行。\n");
 
    // 1）从站点参数文件中加载站点参数，存放于stlist容器中；
    if (loadstcode(argv[1]) ==  false) EXIT(-1);   

    // 2）根据站点参数，生成站点观测数据（随机数）
    memset(strddatetime,0,sizeof(strddatetime));
    ltime(strddatetime,"yyyymmddhh24miss"); 
    strncpy(strddatetime+12,"00",2);                  
    crtsurfdata();

    // 3）把容器datalist中的气象观测数据写入文件
    if (strstr(argv[4],"csv")!=0)    crtsurffile(argv[2],"csv");
    if (strstr(argv[4],"xml")!=0)   crtsurffile(argv[2],"xml");
    
    logfile.write("crtsurfdata 运行结束。\n");
    return 0;
}
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);

    exit(0);
}
bool loadstcode(const string &inifile)   
{
    cifile ifile;    
    string strbuffer;
    ccmdstr cmdstr;                   
    st_stcode stcode;    
    if (ifile.open(inifile)==false) 
    {
        logfile.write("ifile.open(%s) failed.\n",inifile.c_str()); return false;
    }
    ifile.readline(strbuffer);           
    while(ifile.readline(strbuffer))
    {
        cmdstr.splittocmd(strbuffer,",");         
        memset(&stcode,0,sizeof(st_stcode));
        cmdstr.getvalue(0,stcode.provname,30);   
        cmdstr.getvalue(1,stcode.obtid,10);          
        cmdstr.getvalue(2,stcode.obtname,30);    
        cmdstr.getvalue(3,stcode.lat);                 
        cmdstr.getvalue(4,stcode.lon);                  
        cmdstr.getvalue(5,stcode.height);            
        stlist.push_back(stcode);                       
    }
    return true;
}
void crtsurfdata()
{
    srand(time(0));             
    st_surfdata stsurfdata; 
    for (auto &aa : stlist)
    {
        memset(&stsurfdata,0,sizeof(st_surfdata));
        strcpy(stsurfdata.obtid,aa.obtid);                      
        strcpy(stsurfdata.ddatetime,strddatetime);      
        stsurfdata.t=rand()%350;                                 
        stsurfdata.p=rand()%265+10000;                     
        stsurfdata.u=rand()%101;                                 
        stsurfdata.wd=rand()%360;                               
        stsurfdata.wf=rand()%150;                               
        stsurfdata.r=rand()%16;                                 
        stsurfdata.vis=rand()%5001+100000;            
        datalist.push_back(stsurfdata);                                                 
    }
}
bool crtsurffile(const string& outpath,const string& datafmt)
{
  	string strfilename;
    cofile ofile; 
    strfilename=outpath+"/"+"SURF_ZH_"+
            strddatetime+"_"+to_string(getpid())+"."+datafmt;

    if (ofile.open(strfilename)==false)
    {
        logfile.write("ofile.open(%s) failed.\n",strfilename.c_str()); return false;
    }

    // 把datalist容器中的观测数据写入文件，支持csv、xml和json三种格式
    if (datafmt=="csv")    
        ofile.writeline("站点代码,数据时间,气温,气压,相对湿度,风向,风速,降雨量,能见度\n");
     if (datafmt=="xml")   
         ofile.writeline("<data>\n");

    for (auto &aa : datalist)
    {
        // 把每行数据写入文件
         if (datafmt=="csv") 
 ofile.writeline("%s,%s,%.1f,%.1f,%d,%d,%.1f,%.1f,%.1f\n",
                            aa.obtid,
                            aa.ddatetime,
                            aa.t/10.0,
                            aa.p/10.0,
                            aa.u,
                            aa.wd,
                            aa.wf/10.0,
                            aa.r/10.0,
                            aa.vis/10.0);
       
        if (datafmt=="xml") 
            ofile.writeline("<obtid>%s</obtid><ddatetime>%s</ddatetime><t>%.1f</t><p>%.1f</p><u>%d</u>"\
                                   "<wd>%d</wd><wf>%.1f</wf><r>%.1f</r><vis>%.1f</vis><endl/>\n",\
                            aa.obtid,
                            aa.ddatetime,
                            aa.t/10.0,
                            aa.p/10.0,
                            aa.u,
                            aa.wd,
                            aa.wf/10.0,
                            aa.r/10.0,
                            aa.vis/10.0);
        
    }
    if (datafmt=="xml")  
        ofile.writeline("</data>\n");

    // 关闭临时文件，并改名为正式的文件
    ofile.closeandrename();    
    logfile.write("生成数据文件%s成功，数据时间%s，记录数%d。\n",strfilename.c_str(),strddatetime,datalist.size());

    return true;
}
```

编译源码，并运行`crtsurfdata` .并查看生成的xml文件，可以看到是符合我们业务预期的。

```shell
[root@localhost cpp]# rm -rf /log/idc/crtsurfdata.log 
[root@localhost cpp]# /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log csv,xml,json
[root@localhost cpp]# cd /tmp/idc/surfdata/
[root@localhost surfdata]# ls
SURF_ZH_20240330205800_116426.csv  SURF_ZH_20240330205800_116426.xml
[root@localhost surfdata]# more SURF_ZH_20240330205800_116426.xml 
<data>
<obtid>58015</obtid><ddatetime>20240330205800</ddatetime><t>14.6</t><p>1024.6</p><u>78</u><wd>38</wd><wf>8.0</wf><r>0.4</r><vis>10150.4</vis><endl/>
<obtid>58102</obtid><ddatetime>20240330205800</ddatetime><t>21.3</t><p>1021.0</p><u>65</u><wd>318</wd><wf>13.9</wf><r>0.1</r><vis>10316.5</vis><endl/>
<obtid>58118</obtid><ddatetime>20240330205800</ddatetime><t>25.9</t><p>1020.3</p><u>52</u><wd>140</wd><wf>6.4</wf><r>0.7</r><vis>10158.5</vis><endl/>
<obtid>58122</obtid><ddatetime>20240330205800</ddatetime><t>26.2</t><p>1000.0</p><u>13</u><wd>197</wd><wf>5.3</wf><r>0.6</r><vis>10298.7</vis><endl/>
<obtid>58203</obtid><ddatetime>20240330205800</ddatetime><t>27.5</t><p>1024.7</p><u>100</u><wd>44</wd><wf>5.4</wf><r>0.2</r><vis>10094.6</vis><endl/>
...
</data>
[root@localhost surfdata]# more /log/idc/crtsurfdata.log 
2024-03-30 20:58:57 crtsurfdata 开始运行。
2024-03-30 20:58:57 生成数据文件/tmp/idc/surfdata/SURF_ZH_20240330205800_116426.csv成功，数据时间20240330205800，记录数839。
2024-03-30 20:58:57 生成数据文件/tmp/idc/surfdata/SURF_ZH_20240330205800_116426.xml成功，数据时间20240330205800，记录数839。
2024-03-30 20:58:57 crtsurfdata 运行结束。
[root@localhost surfdata]# 
```

:::

#### 写成json格式

:::details 落地实现

```c{199-209}
/*
 *  程序名：crtsurfdata.cpp  本程序用于生成气象站点观测的分钟数据。
 *  作者：吴从周。
*/
#include "_public.h"
using namespace idc;
struct st_stcode;// 站点参数的结构体
struct st_surfdata;// 气象站观测数据的结构体
struct st_stcode    
{
    char provname[31]; // 省
    char obtid[11]; // 站号
    char obtname[31]; // 站名，同城市名。
    double lat;// 纬度：度。
    double lon;// 经度：度。
    double height; // 海拔高度：米。
};

struct st_surfdata
{
    char obtid[11]; // 站点代码。
    char ddatetime[15];  // 数据时间：格式yyyymmddhh24miss，精确到分钟，秒固定填00。
    int  t;// 气温：单位，0.1摄氏度。
    int  p;// 气压：0.1百帕。
    int  u;// 相对湿度，0-100之间的值。
    int  wd;// 风向，0-360之间的值。
    int  wf;// 风速：单位0.1m/s
    int  r; // 降雨量：0.1mm。
    int  vis;// 能见度：0.1米。
};
// 存观测数据的容器
list<struct st_surfdata>  datalist;         
// 存放全部的站点参数
list<struct st_stcode> stlist;
char strddatetime[15];   
clogfile logfile;      

/*===========把容器datalist中的气象观测数据写入文件==========
 	const string& outpath	//<I> 数据文件存放的目录
 	const string& datafmt	//<I> 数据文件的格式 取值：csv、xml和json
*/
bool crtsurffile(const string& outpath,const string& datafmt);
bool loadstcode(const string &inifile);  
void crtsurfdata(); 
void EXIT(int sig);    
int main(int argc,char *argv[])
{

    if (argc!=5)
    {
        // 如果参数非法，给出帮助文档。
        cout << "Using:./crtsurfdata inifile outpath logfile datafmt\n";
        cout << "Examples:/project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log csv,xml,json\n\n";

        cout << "inifile  气象站点参数文件名。\n";
        cout << "outpath  气象站点数据文件存放的目录。\n";
        cout << "logfile  本程序运行的日志文件名。\n";
        cout << "datafmt  输出数据文件的格式，支持csv、xml和json，中间用逗号分隔。\n\n";
        return -1;  
    }
	//忽略所有的信号和IO
    closeioandsignal(true);    
    signal(SIGINT,EXIT); signal(SIGTERM,EXIT);
    if (logfile.open(argv[3])==false)
    {
        cout << "logfile.open(" << argv[3] << ") failed.\n";  			return -1;
    }

    logfile.write("crtsurfdata 开始运行。\n");
    
    // 在这里编写处理业务的代码。
    // 1）从站点参数文件中加载站点参数，存放于stlist容器中；
    if (loadstcode(argv[1]) ==  false) EXIT(-1);   

    // 2）根据站点参数，生成站点观测数据（随机数）
    memset(strddatetime,0,sizeof(strddatetime));
    ltime(strddatetime,"yyyymmddhh24miss");  
    strncpy(strddatetime+12,"00",2);                   
    crtsurfdata();

    // 3）把容器datalist中的气象观测数据写入文件
    if (strstr(argv[4],"csv")!=0)    crtsurffile(argv[2],"csv");
    if (strstr(argv[4],"xml")!=0)   crtsurffile(argv[2],"xml");
    if (strstr(argv[4],"json")!=0)  crtsurffile(argv[2],"json");
    logfile.write("crtsurfdata 运行结束。\n");
    return 0;
}
void EXIT(int sig)
{
    logfile.write("程序退出，sig=%d\n\n",sig);
    exit(0);
}
bool loadstcode(const string &inifile)   
{
    cifile ifile; 
    string strbuffer;    
    ccmdstr cmdstr;                  
    st_stcode stcode;  
    if (ifile.open(inifile)==false) 
    {
        logfile.write("ifile.open(%s) failed.\n",inifile.c_str()); return false;
    }
    ifile.readline(strbuffer);             
    while(ifile.readline(strbuffer))
    {
        cmdstr.splittocmd(strbuffer,",");      
        memset(&stcode,0,sizeof(st_stcode));
        cmdstr.getvalue(0,stcode.provname,30);   
        cmdstr.getvalue(1,stcode.obtid,10);         
        cmdstr.getvalue(2,stcode.obtname,30);   
        cmdstr.getvalue(3,stcode.lat);                   
        cmdstr.getvalue(4,stcode.lon);                  
        cmdstr.getvalue(5,stcode.height);            
        stlist.push_back(stcode);                           
    }
    return true;
}
void crtsurfdata()
{
    srand(time(0));             
    st_surfdata stsurfdata;   
    for (auto &aa : stlist)
    {
        memset(&stsurfdata,0,sizeof(st_surfdata));
        strcpy(stsurfdata.obtid,aa.obtid);                      
        strcpy(stsurfdata.ddatetime,strddatetime);       
        stsurfdata.t=rand()%350;                                 
        stsurfdata.p=rand()%265+10000;                   
        stsurfdata.u=rand()%101;                                 
        stsurfdata.wd=rand()%360;                               
        stsurfdata.wf=rand()%150;                              
        stsurfdata.r=rand()%16;                                 
        stsurfdata.vis=rand()%5001+100000;          
        datalist.push_back(stsurfdata);                         
    }
}

bool crtsurffile(const string& outpath,const string& datafmt)
{
   
    string strfilename; 
    cofile ofile;     
    
     // 拼接生成数据的文件名
    strfilename=outpath+"/"+"SURF_ZH_"+
            strddatetime+"_"+to_string(getpid())+"."+datafmt;
   
    if (ofile.open(strfilename)==false)
    {
        logfile.write("ofile.open(%s) failed.\n",strfilename.c_str()); return false;
    }

    // 把datalist容器中的观测数据写入文件，支持csv、xml和json三种格式
    if (datafmt=="csv")    ofile.writeline("站点代码,数据时间,气温,气压,相对湿度,风向,风速,降雨量,能见度\n");
    if (datafmt=="xml")   ofile.writeline("<data>\n");
    if (datafmt=="json")  ofile.writeline("{\"data\":[\n");

    // 遍历存放观测数据的datalist容器
    for (auto &aa : datalist)
    {
        // 把每行数据写入文件
         if (datafmt=="csv") 
            ofile.writeline("%s,%s,%.1f,%.1f,%d,%d,%.1f,%.1f,%.1f\n",\
                                    aa.obtid,
                            		aa.ddatetime,
                            		aa.t/10.0,
                            		aa.p/10.0,
                            		aa.u,
                            		aa.wd,
                            		aa.wf/10.0,
                            		aa.r/10.0,
                            		aa.vis/10.0);
        if (datafmt=="xml") 
            ofile.writeline("<obtid>%s</obtid><ddatetime>%s</ddatetime><t>%.1f</t><p>%.1f</p><u>%d</u>"\
                                   "<wd>%d</wd><wf>%.1f</wf><r>%.1f</r><vis>%.1f</vis><endl/>\n",\
                            aa.obtid,
                            aa.ddatetime,
                            aa.t/10.0,
                            aa.p/10.0,
                            aa.u,
                            aa.wd,
                            aa.wf/10.0,
                            aa.r/10.0,
                            aa.vis/10.0);
      
        if (datafmt=="json") 
            ofile.writeline("{\"obtid\":\"%s\",\"ddatetime\":\"%s\",\"t\":\"%.1f\",\"p\":\"%.1f\","\
                                  "\"u\":\"%d\",\"wd\":\"%d\",\"wf\":\"%.1f\",\"r\":\"%.1f\",\"vis\":\"%.1f\"}",\
                            aa.obtid,
                            aa.ddatetime,
                            aa.t/10.0,
                            aa.p/10.0,
                            aa.u,
                            aa.wd,
                            aa.wf/10.0,
                            aa.r/10.0,
                            aa.vis/10.0);
        
        // 注意，json文件的最后一条记录不需要逗号，用以下代码特殊处理
        static int ii=0;     // 已写入数据行数的计数器
        if (ii<datalist.size()-1)
        {
            // 如果不是最后一行。
            ofile.writeline(",\n");  ii++;
        }
        else
        {
              ofile.writeline("\n");  
        }
        
    }

    if (datafmt=="xml")  ofile.writeline("</data>\n");
    if (datafmt == "json") ofile.writeline("]}\n");
    
    // 关闭临时文件，并改名为正式的文件
    ofile.closeandrename();   
    logfile.write("生成数据文件%s成功，数据时间%s，记录数%d。\n",strfilename.c_str(),strddatetime,datalist.size());
    return true;
}
```

```shell
[root@localhost cpp]# make
g++ -g -o crtsurfdata crtsurfdata.cpp -I/project/public /project/public/_public.cpp 
cp -f crtsurfdata ../bin/.
[root@localhost cpp]# rm -rf  /log/idc/crtsurfdata.log
[root@localhost cpp]#  /project/idc/bin/crtsurfdata /project/idc/ini/stcode.ini /tmp/idc/surfdata /log/idc/crtsurfdata.log csv,xml,json
[root@localhost cpp]# more /log/idc/crtsurfdata.log
2024-03-30 21:08:27 crtsurfdata 开始运行。
2024-03-30 21:08:27 生成数据文件/tmp/idc/surfdata/SURF_ZH_20240330210800_10516.csv成功，数据时间20240330210800，记录数839。
2024-03-30 21:08:27 生成数据文件/tmp/idc/surfdata/SURF_ZH_20240330210800_10516.xml成功，数据时间20240330210800，记录数839。
2024-03-30 21:08:27 生成数据文件/tmp/idc/surfdata/SURF_ZH_20240330210800_10516.json成功，数据时间20240330210800，记录数839。
2024-03-30 21:08:27 crtsurfdata 运行结束。
[root@localhost cpp]# more /tmp/idc/surfdata/SURF_ZH_20240330210800_10516.json
{"data":[
{"obtid":"58015","ddatetime":"20240330210800","t":"7.8","p":"1017.5","u":"65","wd":"44","wf":"6.6","r":"0.4","vis":"10124.8"}
{"obtid":"58102","ddatetime":"20240330210800","t":"10.0","p":"1007.6","u":"33","wd":"82","wf":"6.0","r":"1.0","vis":"10474.0"}
...
```

:::
